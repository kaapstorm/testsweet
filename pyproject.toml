[project]
name = "Assertions"
version = "0.1.0"
description = "Python testing for humans"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "ruff",
]

[tool.ruff]
line-length = 79

[tool.ruff.format]
quote-style = "single"
